DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
# Contraseña asignada a las cuentas que se auto-crean al registrar una Persona con correo,
# y usada por seed_users. Cambia este valor (y SECRET_KEY) antes de desplegar a un entorno real.
DEFAULT_USER_PASSWORD=demo123

DB_NAME=fepi_contracts
DB_USER=fepi
DB_PASSWORD=fepi
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000
